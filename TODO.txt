Overall Goals (cross-session):
1. Get prepared dataset 
2. Probing experiments & analysis
3. Baseline training experiment (block diffusion depth_decoder)


---

Prompt spec #1: Dataprep setup for Miso/Qwen3/Fish

Data / Feature extraction modules needed
- Encode audio to stacked codebooks (F, C) dump, containing int codebook indexes where F=num_time_frames, C=num_codebooks (semantic + audio)
- Make (text, audio) token sequences
- Teacher force model forward with logits/logprobs dump
- Teacher force with additional hidden states / kv context dump

Generate (context, text) completion audio as stacked codebooks dump
Decode audio from stacked codebooks dump

Start with 3 examples from expresso dataset, later can extend to other datasets.
Any dataset essentially needs {audio, transcript_segments.json} where text segments are marked by [start, end] times

[rough]
Need to start with data preparation/feature extraction utilities, similar to what was done
in `MisoTTS/scripts/measure_miso_mimi_codebook_entropy.py`

We'll work on branch akash/dataprep with project structure as:

dflash-voice
    dataprep
        *.py  (scripts, utils)
    data
        expresso/
            raw/ROW/
                audio.wav, transcript_segments.json
            MODEL_tokenized/ROW/
                codebooks.pt (F, C)
                sequences.pt (L, C+1), tokenized concat sequences of L=N+F with N,1 text F,C audio codes in separate channels 
                    either individual segments or chunks of interleaved segments upto max_seq_length
                metadata.json, mapping sequence id and position in sequence back to attributes from raw data
            MODEL_featurized/ROW/
                logits.pt (L, C, vocab), hiddens.pt (L, C, d_model)
                kv_context.pt (layers, L, d_kv, 2)

Create a [dataprep] dep group for torch and others needed. We don't use HF datasets parquet as yet, simple .pt tensor files

We can start with the MisoTTS model, then extend to Qwen3 and Fish
I would like a shared API of Tokenizer.AudioCodec.encode/decode, Tokenizer.TextTokenizer.encode/decode
and Tokenizer.apply_chat_template(list[segments]) which adds appropriate model-specific text, speaker, bos/eos etc tokens to make a flat sequence
